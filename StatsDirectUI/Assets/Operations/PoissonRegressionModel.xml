<?xml version="1.0" encoding="utf-8"?>
<operation xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns="http://www.statsdirect.com/schemas/Operation.xsd">
	<name>PoissonRegressionModel</name>
	<friendly-name lang="en-GB">Model analysis</friendly-name>
  <prerequisite-operation>PoissonRegression</prerequisite-operation>
  <steps>
		<builtin function-name="rptPoissonRegressionModel" />
    <report type="application/x-statsdirect-creole">
      <![CDATA[<report>
<title>Poisson regression - model analysis</title>

<p>Accuracy = @acc<br />
Log likelihood with all covariates = @ll</p>

<p>Deviance with all covariates = @dev   df = @idf   rank = @rank<br />
Akaike information criterion = @aka<br />
Schwartz information criterion = @sch</p>

<p>Deviance with no covariates = @devx<br />
Deviance (likelihood ratio, G&#xb2;) = @x2dev   df = @x2df   <pval>@{x2p:pval}</pval><br />
Pseudo (McFadden) R-square = @r2<br />
Pseudo (likelihood ratio index) R-square = @r2i</p>

<p>Pearson goodness of fit = @x2_pearson   df = @idf   <pval>@{p_pearson:pval}</pval><br />
Deviance goodness of fit = @dev   df = @idf   <pval>@{p_dev:pval}</pval></p>

<p>Over-dispersion scale parameter = @sp<br />
Scaled G&#xb2; = @x2dev_scaled   df = @x2df   <pval>@{x2p_scaled:pval}</pval><br />
Scaled Pearson goodness of fit = @x2_scaled   df = @idf   <pval>@{p_p_scaled:pval}</pval><br />
Scaled Deviance goodness of fit = @dev_scaled   df = @idf   <pval>@{p_dev_scaled:pval}</pval></p>

<table>
<tr><th>Parameter</th><th>Coefficient</th><th>Standard Error</th></tr>
<block name="unscaled"><tr><td>@par</td><td>@coef</td><td>@err</td></tr></block>
</table>
<br />
<table>
<tr><th>Parameter</th><th>Scaled Standard Error</th><th>Scaled Wald z</th><th></th></tr>
<block name="scaled"><tr><td>@par</td><td>@err</td><td>@z</td><td><pval>@{p:pval}</pval></td></tr></block>
</table>
</report>]]>
    </report>
  </steps>
</operation>
