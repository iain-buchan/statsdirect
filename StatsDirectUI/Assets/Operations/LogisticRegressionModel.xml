<?xml version="1.0" encoding="utf-8"?>
<operation xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns="http://www.statsdirect.com/schemas/Operation.xsd">
	<name>LogisticRegressionModel</name>
	<friendly-name lang="en-GB">Model analysis</friendly-name>
  <prerequisite-operation>LogisticRegression</prerequisite-operation>
  <steps>
		<builtin function-name="rptLogisticRegressionModel" />
    <report type="application/x-statsdirect-creole">
      <![CDATA[<report>
<title>Logistic regression - model analysis</title>

<p>Accuracy = @acc<br />
Log likelihood with all covariates = @ll</p>

<p>Deviance with all covariates = @dev   df = @idf   rank = @rank<br />
Akaike information criterion = @aka<br />
Schwartz information criterion = @sch</p>

<p>Deviance with no covariates = @devx<br />
Deviance (likelihood ratio) chi-square = @x2dev   df = @x2df   <pval>@{x2p:pval}</pval><br />
Pseudo (McFadden) R-square = @r2<br />
Pseudo (likelihood ratio index) R-square = @r2i</p>

<p>Pearson chi-square goodness of fit = @x2   df = @idf   <pval>@{p:pval}</pval><br />
Deviance goodness of fit = @dev_good   df = @df_good   <pval>@{p_good:pval}</pval><br />
Hosmer-Lemeshow test = @hl   df = @df_hl   <pval>@{p_hl:pval}</pval></p>

<table>
<tr><th>Parameter</th><th>Coefficient</th><th>Standard Error</th><th>Z Value</th><th><pval>P(>|Z|)</pval></th></tr>
<block name="parameters"><tr><td>@par</td><td>@coef</td><td>@err</td><td>@z</td><td><pval>@{p_var:pval}</pval></td></tr>
</block>
</table>
</report>]]>
    </report>
  </steps>
</operation>
